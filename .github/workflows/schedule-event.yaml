name: Schedule Event
on:
 schedule:
  - cron: '*/2 * * * *'
jobs:
  schedule-job:
    runs-on: ubuntu-latest  
    steps:
    - name: Print Schedule Info
      run: echo "Schedule Event Workflow"